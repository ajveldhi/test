
filebeat.inputs:
  - type: log
    enabled: true
    paths:
      - /var/log/syslog
      - /var/log/auth.log
      - /var/log/messages
    encoding: utf-8

output.logstash:
  hosts: ["logstash:5044"]
